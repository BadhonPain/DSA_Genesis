#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    // library functions
    cout << sqrt(10) << endl;
    cout << cbrt(10) << endl;
    cout << min(4, 8) << endl; // for min and max -> cmath not needed
    cout << max(4, 8) << endl;
    cout << pow(2, 10) << endl;
}